{
  "name": "timer-app",
  "version": "1.0.0",
  "description": "A Timer App",
  "main": "app.js",
  "scripts": {
    "test": "mocha",
    "dev": "nodemon app/app.js",
    "start": "pm2 start app/config/env.config.js --env production",
    "restart": "pm2 restart app/config/env.config.js --env production"
  },
  "private": true,
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "nodemon": "^2.0.18",
    "pm2": "^5.2.0"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "chai-http": "^4.3.0",
    "mocha": "^10.0.0"
  }
}
